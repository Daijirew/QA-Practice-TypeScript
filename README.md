# Sauce Demo - ระบบทดสอบอัตโนมัติด้วย Playwright POM

ระบบทดสอบอัตโนมัติแบบครอบคลุมสำหรับ https://www.saucedemo.com/ โดยใช้ Playwright, TypeScript และ Page Object Model (POM)

## 📋 โครงสร้างโปรเจค

```
QA-Practice-TS/
├── pages/                    # คลาส Page Object Model
│   ├── LoginPage.ts         # การทำงานของหน้า Login
│   ├── ProductsPage.ts      # การทำงานของหน้ารายการสินค้า
│   ├── CartPage.ts          # การทำงานของหน้าตะกร้าสินค้า
│   ├── CheckoutPage.ts      # การทำงานของหน้าชำระเงิน
│   └── ProductDetailPage.ts # การทำงานของหน้ารายละเอียดสินค้า
├── fixtures/                 # Playwright fixtures
│   └── pageFixture.ts       # Page object fixtures
├── tests/                    # ไฟล์ Test
│   ├── login.spec.ts        # ทดสอบการ Login
│   ├── products.spec.ts     # ทดสอบหน้าสินค้า
│   ├── cart.spec.ts         # ทดสอบตะกร้าสินค้า
│   ├── checkout.spec.ts     # ทดสอบการชำระเงิน
│   ├── productDetail.spec.ts # ทดสอบรายละเอียดสินค้า
│   └── e2e.spec.ts          # ทดสอบ End-to-end
├── playwright.config.ts      # การตั้งค่า Playwright
├── tsconfig.json            # การตั้งค่า TypeScript
└── package.json             # Dependencies ของโปรเจค
```

## 🚀 เริ่มต้นใช้งาน

### ความต้องการเบื้องต้น
- Node.js (v14 หรือสูงกว่า)
- npm หรือ yarn

### การติดตั้ง

1. ติดตั้ง dependencies:
```bash
npm install
```

2. ติดตั้ง Playwright browsers:
```bash
npx playwright install
```

## 🧪 การรันเทส

### รันเทสทั้งหมด:
```bash
npm test
```

### รันเทสในโหมด UI:
```bash
npm run test:ui
```

### รันเทสแบบเห็น browser (headed mode):
```bash
npm run test:headed
```

### รันเทสในโหมด debug:
```bash
npm run test:debug
```

### ดูรีพอร์ตการทดสอบ:
```bash
npm run test:report
```

## 📝 การครอบคลุมการทดสอบ

### การทดสอบ Login (`login.spec.ts`)
- ✓ แสดงองค์ประกอบหน้า login
- ✓ Login สำเร็จด้วยข้อมูลที่ถูกต้อง
- ✓ แสดงข้อความ error เมื่อ username ผิด
- ✓ แสดงข้อความ error เมื่อ password ผิด
- ✓ จัดการกับ user ที่ถูกล็อค
- ✓ Login ด้วย problem user
- ✓ Login ด้วย performance glitch user

### การทดสอบหน้าสินค้า (`products.spec.ts`)
- ✓ แสดงรายการสินค้า
- ✓ ตรวจสอบว่ามี 6 สินค้า
- ✓ ดึงชื่อสินค้าแรก
- ✓ เพิ่มสินค้าชิ้นเดียวในตะกร้า
- ✓ เพิ่มหลายสินค้าในตะกร้า
- ✓ ลบสินค้าจากตะกร้า
- ✓ ไปที่หน้าตะกร้า
- ✓ ดึงราคาสินค้าทั้งหมด
- ✓ ดึงชื่อสินค้าทั้งหมด
- ✓ เรียงตามชื่อ (A ถึง Z)
- ✓ เรียงตามชื่อ (Z ถึง A)
- ✓ เรียงตามราคา (ต่ำไปสูง)
- ✓ เรียงตามราคา (สูงไปต่ำ)
- ✓ Logout จากเมนู hamburger

### การทดสอบตะกร้าสินค้า (`cart.spec.ts`)
- ✓ แสดงตะกร้าว่าง
- ✓ เพิ่มสินค้าชิ้นเดียว
- ✓ เพิ่มหลายสินค้า
- ✓ ดึงชื่อสินค้าในตะกร้า
- ✓ ดึงราคาสินค้าในตะกร้า
- ✓ ลบสินค้าจากตะกร้า
- ✓ กลับไปช้อปปิ้งต่อ
- ✓ คำนวณราคารวม
- ✓ ไปที่หน้าชำระเงิน
- ✓ ล้างสินค้าทั้งหมดจากตะกร้า

### การทดสอบการชำระเงิน (`checkout.spec.ts`)
- ✓ แสดงองค์ประกอบหน้าชำระเงิน
- ✓ กรอกข้อมูลการชำระเงิน
- ✓ ไปยังหน้าตรวจสอบ
- ✓ แสดงราคาสินค้ารวม
- ✓ แสดงภาษี
- ✓ แสดงราคารวมทั้งหมด
- ✓ ตรวจสอบการคำนวณราคา
- ✓ ยกเลิกการชำระเงิน
- ✓ ชำระเงินสำเร็จ
- ✓ แสดงข้อความสำเร็จ
- ✓ ชำระเงินด้วยข้อมูลผู้ใช้ที่แตกต่างกัน

### การทดสอบรายละเอียดสินค้า (`productDetail.spec.ts`)
- ✓ ไปยังหน้ารายละเอียดสินค้า
- ✓ แสดงข้อมูลสินค้า
- ✓ เพิ่มสินค้าในตะกร้าจากหน้ารายละเอียด
- ✓ แสดงปุ่มลบหลังจากเพิ่ม
- ✓ ลบสินค้าจากหน้ารายละเอียด
- ✓ กลับไปหน้าสินค้า
- ✓ ไปยังตะกร้าจากหน้ารายละเอียด
- ✓ ตรวจสอบว่าสินค้าทั้งหมดมีหน้ารายละเอียด
- ✓ แสดงราคาที่ถูกต้องในหน้ารายละเอียด

### การทดสอบ End-to-End (`e2e.spec.ts`)
- ✓ ทำขั้นตอนการซื้อทั้งหมด
- ✓ จัดการการอัปเดตตะกร้าระหว่างช้อปปิ้ง
- ✓ ตรวจสอบความพร้อมของสินค้าในหลายหน้า
- ✓ จัดการหลายสถานการณ์การชำระเงิน

## 🔑 ข้อมูลสำหรับทดสอบ

ผู้ใช้ทดสอบสำหรับ https://www.saucedemo.com/:

| ชื่อผู้ใช้ | รหัสผ่าน | รายละเอียด |
|----------|----------|-------------|
| standard_user | secret_sauce | บัญชีผู้ใช้มาตรฐาน |
| locked_out_user | secret_sauce | บัญชีที่ไม่สามารถ login ได้ |
| problem_user | secret_sauce | ผู้ใช้ที่มีปัญหาการแสดงผล |
| performance_glitch_user | secret_sauce | ผู้ใช้ที่มีปัญหาความเร็ว |

## 📊 การตั้งค่าการทดสอบ

### รองรับ Browser
- Chromium (Desktop Chrome)
- Firefox (Desktop Firefox)
- WebKit (Desktop Safari)
- Mobile Chrome (Pixel 5)

### การรายงานผล
- รีพอร์ต HTML พร้อม screenshots และวิดีโอ
- Trace files สำหรับ debug (เมื่อ retry ครั้งแรก)
- จับภาพหน้าจอเมื่อเทสล้มเหลว
- บันทึกวิดีโอเมื่อเทสล้มเหลว

## 🛠️ ประโยชน์ของ Page Object Model

รูปแบบ POM ที่ใช้ในโปรเจคนี้ให้:

1. **ความสามารถในการดูแลรักษา**: Locators ถูกรวมไว้ในคลาส page
2. **การนำกลับมาใช้ใหม่**: การกระทำทั่วไปสามารถแชร์ได้ทั่วทั้งเทส
3. **ความอ่านง่าย**: เทสเน้นที่ business logic ไม่ใช่รายละเอียดการทำงาน
4. **ความสามารถในการขยาย**: ง่ายต่อการเพิ่ม page objects และเทสใหม่
5. **การแยกความรับผิดชอบ**: แยก UI interactions ออกจาก test logic

## 📖 คลาส Page Object

### LoginPage
```typescript
- navigate()
- login(username, password)
- getErrorMessage()
- isErrorMessageVisible()
```

### ProductsPage
```typescript
- getProductCount()
- getFirstProductName()
- addProductToCart(index)
- removeProductFromCart(index)
- getCartBadgeValue()
- goToCart()
- sortProducts(option)
- openHamburgerMenu()
- logout()
- getAllProductPrices()
- getAllProductNames()
```

### CartPage
```typescript
- getCartItemCount()
- getCartItemNames()
- getCartItemPrices()
- removeCartItem(index)
- continueShopping()
- checkout()
- isCartEmpty()
- getTotalPrice()
```

### CheckoutPage
```typescript
- fillCheckoutInfo(firstName, lastName, zipCode)
- clickContinue()
- clickCancel()
- clickFinish()
- completeCheckout(...)
- getTotalPrice()
- getItemTotal()
- getTax()
- isOrderComplete()
- getOrderCompleteMessage()
```

### ProductDetailPage
```typescript
- getProductName()
- getProductPrice()
- getProductDescription()
- addToCart()
- removeFromCart()
- goBack()
- goToCart()
- isAddToCartButtonVisible()
- isRemoveFromCartButtonVisible()
```

## 🔍 การ Debug

### ใช้ Playwright Inspector
```bash
npm run test:debug
```

### ดู test traces
Traces จะถูกบันทึกโดยอัตโนมัติสำหรับเทสที่ล้มเหลว ดูได้ที่:
```bash
npx playwright show-trace trace.zip
```

## 📚 แนวทางปฏิบัติที่ดี

1. **การแยกข้อมูล**: แต่ละเทสเป็นอิสระและไม่พึ่งพาซึ่งกันและกัน
2. **การรอแบบชัดเจน**: ใช้กลไกการรอที่มีใน Playwright
3. **การจัดกลุ่มการกระทำ**: การกระทำที่เกี่ยวข้องจะถูกจัดกลุ่มใน page methods
4. **การยืนยัน**: การยืนยันที่ชัดเจนและมีความหมายในเทส
5. **การจัดการ Error**: การจัดการ error ที่เหมาะสมสำหรับความล้มเหลวที่คาดไว้

## 🐛 การแก้ไขปัญหา

### เทสล้มเหลวเนื่องจาก timeout
- เพิ่ม timeout ใน `playwright.config.ts`
- ตรวจสอบการเชื่อมต่ออินเทอร์เน็ต
- ตรวจสอบว่าเว็บไซต์เข้าถึงได้

### เทสล้มเหลวเนื่องจาก selectors
- รันเทสในโหมด debug เพื่อตรวจสอบ elements
- อัปเดต locators ใน page objects หาก UI เปลี่ยน
- ใช้ `--debug` flag เพื่อหยุดการทำงานชั่วคราว

## 📄 สิทธิ์การใช้งาน

โปรเจคนี้มีวัตถุประสงค์เพื่อการศึกษาและฝึกฝน

## 👤 Poomrapee Patum

โปรเจคฝึกฝน QA
